# Memory Protocol
- **CRITICAL:** The agent is strictly FORBIDDEN from using the native `save_memory` tool for project context. It is strictly reserved for global user preferences.
- Redirect all project memory operations to the MCP tools (`engram:mem_save` and `engram:mem_search`).
